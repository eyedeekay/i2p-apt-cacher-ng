# Defaults for  initscript
# sourced by /etc/init.d/
# installed at /etc/default/ by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Set to non-empty value to disable the start of the service at boot time
# DISABLED=1

# See umask(2) or chmod(1) for details.
# 002 would make new files writable by group.
#
# umask 022

# Additional options that are passed to the Daemon.
DAEMON_OPTS=" -c /etc/i2p-apt-cacher-ng "
